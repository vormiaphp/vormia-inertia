<!-- vormia-inertia:managed-file app-view -->
<!DOCTYPE html>
<html lang="{{ str_replace('_', '-', app()->getLocale()) }}">
    <head>
        <meta charset="utf-8" />
        <meta name="viewport" content="width=device-width, initial-scale=1" />
{{ react_refresh }}        @vite(['resources/css/app.css', '{{ entry }}'])
        @inertiaHead
    </head>
    <body>
        @inertia
    </body>
</html>
